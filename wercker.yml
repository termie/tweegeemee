no-response-timeout: 60
command-timeout: 60
build:
  box: python:2.7
  steps:
    - pip-install
    - script:
        code: |
          python gen_code.py > src/tweegeemee/onetime.clj

gen-image:
  box: clojure
  steps:
    - script:
        code: |
          lein run -m tweegeemee.onetime/mymain
          ls -lah
          cp output.png $WERCKER_REPORT_ARTIFACTS_DIR/
          cp output.png $WERCKER_OUTPUT_DIR/
